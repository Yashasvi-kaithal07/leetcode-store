                if (nums[i] == nums[j]) {
            for (int j = 0; j < n; j++) {
            int c = 0;
        for (int i = 0; i < n; i++) {

        vector<int> f(n, 0);
    int n = nums.size();
    bool containsDuplicate(vector<int>& nums) {
public:
class Solution {
